{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start-blog",
      "type": "shell",
      "command": ". ~/.zshrc && cd ~/Documents/VSCode/Blog/cgruver/ && bundle && bundle exec jekyll serve --livereload --drafts --unpublished"
    }
  ]
}